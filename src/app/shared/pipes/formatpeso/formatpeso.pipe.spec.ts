import { FormatpesoPipe } from './formatpeso.pipe';

describe('FormatpesoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatpesoPipe();
    expect(pipe).toBeTruthy();
  });
});
